language: node_js

node_js:
  - 11

cache:
  directories:
    - $HOME/.npm

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  on:
    branch: master